# Plan

This package is still a scaffold.

## Near-term shape

- Replace the sample `Calculator` API with the real package API.
- Keep the first version Flutter-native and small.
- Add focused tests alongside real behavior.

## Non-goals for now

- No speculative abstraction layers.
- No extra dependencies unless Flutter or Dart cannot reasonably cover the job.
